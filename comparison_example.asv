X = randn(100,150);
r = zeros(size(X,2),1);
r(1:5)= [0;2;0;-3;0]; % only two nonzero coefficients
y = X*r + randn(100,1)*.1; % small added noise

t = 0.5;
lambda2 = 0.1;

alpha = t/lambda2;

B = lasso(X,y,'alpha',alpha);
find(B(:,1) ~= 0)

beta = SVEN(X',y',t,lambda2);
find(beta ~= 0)
